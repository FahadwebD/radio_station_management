import React from 'react';
import AddStation from '../AddStation';

const Management = () => {
    return (
        <div>
            <AddStation></AddStation>
            <h1 style={{color:'white'}}>Hi from management</h1>
        </div>
    );
};

export default Management;